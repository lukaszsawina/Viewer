<Window x:Class="Viewer.View.VisualisationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
        xmlns:s="https://github.com/canton7/Stylet"
        
        Title="PLY Viewer" Height="450" Width="800">
    <Grid>
        <HelixToolkit:HelixViewport3D x:Name="helixViewport" Grid.Row="0">
            <HelixToolkit:HelixViewport3D.Children>
                <ModelVisual3D Content="{Binding Mesh}" />
            </HelixToolkit:HelixViewport3D.Children>
        </HelixToolkit:HelixViewport3D>

        <!-- Przycisk do załadowania pliku PLY -->
        <Button Content="Load PLY" VerticalAlignment="Top" HorizontalAlignment="Left" Width="100" Height="30" Margin="10" Command="{s:Action LoadPLYCommand}" />
    </Grid>
</Window>
